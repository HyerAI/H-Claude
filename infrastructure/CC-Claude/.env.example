# CC-Claude Configuration (CLI Mode V2.0)
# Copy this file to .env
#
# This proxy uses Claude CLI subprocess instead of API.
# No API key required - uses local Claude CLI authentication (subscription).
#
# Prerequisites:
# - Claude CLI installed and authenticated
# - Active Claude subscription (Max/Pro)

# Default model (alias like 'sonnet', 'opus' or full name)
# Options: sonnet, opus, claude-sonnet-4-20250514, claude-opus-4-20250514
CC_CLAUDE_MODEL=sonnet

# Port (defaults to 2408)
CC_CLAUDE_PORT=2408

# Timeout for CLI subprocess in milliseconds (defaults to 300000 = 5 min)
CC_CLAUDE_TIMEOUT=300000
