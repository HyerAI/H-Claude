# plans/ - DEPRECATED

> **DEPRECATION NOTICE (2026-01-02)**
>
> This folder was used by the deprecated `/hc-plan` command.
>
> Plans are now generated by `/think-tank` and stored in think-tank subject folders:
> ```
> .claude/PM/think-tank/{topic}_{date}/execution-plan.yaml
> ```
>
> This folder is preserved for backwards compatibility with existing projects.
> New plans should use the think-tank workflow.
